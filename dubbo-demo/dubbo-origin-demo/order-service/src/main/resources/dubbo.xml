<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:dubbo="http://dubbo.apache.org/schema/dubbo"
       xmlns="http://www.springframework.org/schema/beans"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.3.xsd
       http://dubbo.apache.org/schema/dubbo http://dubbo.apache.org/schema/dubbo/dubbo.xsd">

    <!-- consumer's application name, used for tracing dependency relationship (not a matching criterion),
    don't set it same as provider -->
    <dubbo:application name="order-service"/>
    <!-- use multicast registry center to discover service -->
    <dubbo:registry id="gg1" address="zookeeper://127.0.0.1:2181"/>
    <dubbo:registry id="gg2" address="zookeeper://127.0.0.1:2182"/>
    <dubbo:registry address="zookeeper://127.0.0.1:2181"/>
    <!-- generate proxy for the remote service, then demoService can be used in the same way as the
    local regular interface -->
    <dubbo:reference id="userService" check="false" interface="one.zpq.service.IUserService" timeout="1000" retries="5" loadbalance="custom" stub="one.zpq.service.UserServiceStub"/>
    <dubbo:reference id="accService" check="false" interface="one.zpq.service.AccountService" timeout="6000" retries="5"/>
</beans>